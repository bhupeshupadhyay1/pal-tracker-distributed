apply from: "$projectDir/../server.gradle"

dependencies {
    compile project(":components:allocations")
//    compile group: 'org.springframework.cloud', name: 'spring-cloud-netflix-eureka-client', version: '2.0.3.RELEASE'
}
